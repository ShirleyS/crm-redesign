App.ApplicationController = Ember.Controller.extend({
  // message: "Hello from ApplicationController"
});



// App.IndexController = Ember.ObjectController.extend({
//   actions : {
//     changeName : function(){
//       // Retrieve the value from the input with name=newName
//       var customer = this.get('customer');
//       // Fetch our model instance
//       var ticket = this.get("ticket");
//       // Update the name property. Using the .set method is required for all the bindings to be automatically updated
//       // song.set("name", songName);
//     },
//   }
// });
